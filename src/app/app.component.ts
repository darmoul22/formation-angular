import { Component } from '@angular/core';

@Component({
  // Metadata configuration for the component
  selector: 'app-root', // The selector used to identify the component in HTML
  templateUrl: './app.component.html', // The HTML template for the component
  styleUrl: './app.component.scss' // The styles for the component
})
export class AppComponent {
  title = 'formation-angular';
}
